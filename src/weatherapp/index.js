import { Map, Chart } from "./components";

export const WeatherApp = () => {
    return (
        <div>
        <h1>Weather App</h1>
        <Map />
        <Chart />

        </div>
    );
}
